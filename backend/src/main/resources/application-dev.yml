spring:
  datasource:
    url: jdbc:postgres://localhost:5517/learnup_backend
    username: backend
    password: backend
  sql:
    init:
      mode: always
  docker:
    compose:
      lifecycle-management: start_only
      file: backend/docker-compose.yml
      enabled: true
